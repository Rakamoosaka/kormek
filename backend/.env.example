# Backend runtime
ENVIRONMENT=development
APP_NAME=Kormek

# Comma-separated origins
CORS_ORIGINS=http://localhost:5173

# Local Docker postgres (project default)
DATABASE_URL=postgresql://kormek:kormek_secret@localhost:5433/kormek
